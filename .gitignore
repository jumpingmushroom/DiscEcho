# Build output
daemon/dist/
daemon/discecho
webui/.svelte-kit/
webui/build/
webui/node_modules/

# Generated
*.test
*.out
coverage.txt
coverage.html

# Local data + secrets
data/
# Negation: daemon/pipelines/data/ is source code, not a data directory.
!daemon/pipelines/data/
config/
.env
.env.local

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Docker volumes mounted by compose
.docker-data/

# Design handoff bundle (reference only, not source)
DiscEcho-handoff.zip

# AI-specific planning artifacts live outside the repo
docs/superpowers/

# Local working agreement; not for the public repo
CLAUDE.md

